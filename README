Author: Kyle Grovers

The FlowerShop program is an example of a Greedy Algorithm, attempting to arrange
data into specified subsets.

INSTALLATION
Ensure you have `bundler` installed (`gem install bundler`)
run `bundle install`

SPECS
run `rspec spec`

EXECUTION
To execute the FlowerShop,
run `rake run` or `rake`

Enter your command at the prompt as per the instructions.
All things being equal, FlowerShop will provide a solution to your order.
